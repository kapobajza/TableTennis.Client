import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    newTeamContainer: {
        justifyContent: 'center',
        padding: 25,
        borderBottomWidth: 1,
        borderBottomColor: '#00acac'
    },
    newTeamButton: {
        margin: 10
    },
    newTeamText: {
        margin: 10,
        textAlign: 'center'
    },
    buttonContainer: { 
        flexDirection: 'row', 
        justifyContent: 'center' 
    }
});

export default styles;