import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    separator: {
        height: 1,
        backgroundColor: '#00acac',
        marginLeft: 10,
        marginRight: 10
    }
});

export default styles;