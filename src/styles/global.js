import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    button: {
        padding: 10,
        borderWidth: 1,
        borderColor: '#0050A0',
        backgroundColor: '#0080FF'
    }
});

export default styles;