import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    container: { 
        margin: 10,
        justifyContent: 'center', 
        padding: 10
    },
    teamName: {
        margin: 25,
        textAlign: 'left',
        fontSize: 25
    },
    button: { 
        position: 'absolute', 
        left: 0, 
        bottom: 0, 
        marginLeft: 25, 
        marginBottom: 10 
    }
});

export default styles;